<view class="container">
	<view class="studentList">
		<block wx:for="{{studentList}}" wx:key="{{item.Id}}">
			<student-item options="{{item}}" bind:view="onViewClass" bind:edit="onEditStudent"></student-item>
		</block>
	</view>
	<view class="addNew" bindtap="onAddnew">添加学生</view>
</view>